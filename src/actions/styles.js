import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  listViewItems: {
    flex: 1,
    justifyContent: 'center',
    minWidth: '25%',
    maxHeight: '50%',
  },
  buffer: {
    height: 50,
  },
});
