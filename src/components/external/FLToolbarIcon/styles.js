import {StyleSheet} from 'react-native';
import colors from 'config/colors';

export const iconColor = colors.ICONS;
export const underlayColor = colors.PRIMARY;
export const iconSize = 27;

export default StyleSheet.create({
  icon: {
    padding: 7,
  },
});
