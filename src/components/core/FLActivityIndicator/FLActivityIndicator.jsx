import React from 'react';
import {ActivityIndicator} from 'react-native';
import {loadingIconColor, loadingIconSize} from './styles';

const FLActivityIndicator = () => <ActivityIndicator size={loadingIconSize} color={loadingIconColor} />;

export default FLActivityIndicator;
